import { db } from 'astro:db';

// seed file for Astro DB
// Astro DB lets you work with a mimicked database locally with seeded data
// good for starting out, but not needed for production
// the data does persist

// https://astro.build/db/seed
export default async function seed() {
	// TODO
}
