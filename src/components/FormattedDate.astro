---

const { intDate, classStuff } = Astro.props;

const formattedDate = new Date(intDate).toLocaleString("en-US", {timeZone: "America/Chicago"});

// FormattedDate
    // just a component to help with rendering a date in a formatted way
---

<time class={classStuff} tabindex="0">{formattedDate}</time>